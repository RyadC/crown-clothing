import { DIV_SpinnerContainer, DIV_SpinnerOverlay } from "./spinner.styles";

const Spinner = () => {
  return (
    <DIV_SpinnerOverlay>
      <DIV_SpinnerContainer />
    </DIV_SpinnerOverlay>
  );
};

export default Spinner;
