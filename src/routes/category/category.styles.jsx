import { styled } from "styled-components";

export const DIV_CategoryContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  column-gap: 20px;
  row-gap: 50px;
`;

export const H1_CategoryTitle = styled.h1`
  text-align: center;
  text-transform: uppercase;
  font-size: 35px;
`;
